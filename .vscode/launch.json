{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Debug active file (LLDB)",
      "type": "cppdbg",
      "request": "launch",
      "program": "${fileDirname}/${fileBasenameNoExtension}.dbg",
      "args": [],
      "cwd": "${fileDirname}",
      "stopAtEntry": false,
      "MIMode": "lldb",
      "preLaunchTask": "build:debug (clang++)"
    },
    {
      "name": "Run release (LLDB)",
      "type": "cppdbg",
      "request": "launch",
      "program": "${fileDirname}/${fileBasenameNoExtension}",
      "args": [],
      "cwd": "${fileDirname}",
      "stopAtEntry": false,
      "MIMode": "lldb",
      "preLaunchTask": "build:release (clang++)"
    }
  ]
}